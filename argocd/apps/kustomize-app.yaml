apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kustomize-maintenance
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/lyhovseab/argocd-apps.git
    targetRevision: HEAD
    path: kustomize/overlays/theta
  destination:
    server: https://kubernetes.default.svc
    namespace: theta
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
