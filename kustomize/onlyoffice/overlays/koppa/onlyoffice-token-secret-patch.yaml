apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: onlyoffice-docserver
spec:
  data:
  - secretKey: jwt-secret
    remoteRef:
      key: "koppa"
      property: "jwt-secret"
